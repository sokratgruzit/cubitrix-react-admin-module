import React from "react";
import styles from "./UsersList.module.css";

const UsersList = () => {
    return (
        <div className={styles.container}>Users list</div>
    );
};

export default UsersList;
